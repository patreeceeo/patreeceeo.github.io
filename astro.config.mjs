// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Content collections are supported natively in Astro v3+ (you are using v5.7.5)
});
